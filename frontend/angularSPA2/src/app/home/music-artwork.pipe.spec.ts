import { MusicArtworkPipe } from './music-artwork.pipe';

describe('MusicArtworkPipe', () => {
  it('create an instance', () => {
    const pipe = new MusicArtworkPipe();
    expect(pipe).toBeTruthy();
  });
});
