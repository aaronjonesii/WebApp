import { FormatMillisecondsToTextPipe } from './format-milliseconds-to-text.pipe';

describe('FormatMillisecondsToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMillisecondsToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
