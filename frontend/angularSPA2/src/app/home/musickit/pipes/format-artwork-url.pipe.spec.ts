import { FormatArtworkUrlPipe } from './format-artwork-url.pipe';

describe('FormatArtworkUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatArtworkUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
